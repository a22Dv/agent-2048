[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agent_2048"
version = "0.1.0"
description = "An program that's able to play the game 2048."

[tool.setuptools]
package-dir = {"" = "src"}